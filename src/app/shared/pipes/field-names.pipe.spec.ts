import { FieldNamesPipe } from './field-names.pipe';

describe('FieldNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
